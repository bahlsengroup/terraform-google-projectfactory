# This file was generated by the bootstrap.sh script from the
# https://github.com/bahlsengroup/terraform-google-projectfactory repository

# This file can be deleted after initial Terraform import is done.

import {
  to = module.project_factory.google_service_account.service_accounts["${SA_SHORT_NAME}"]
  id = "projects/${PROJECT}/serviceAccounts/${SA_FULL_NAME}"
}

import {
  to = google_storage_bucket.tf_state_bucket
  id = "${PROJECT}/${GCS_BUCKET}"
}

import {
  to = module.project_factory.google_project_iam_policy.this
  id = "${PROJECT}"
}
